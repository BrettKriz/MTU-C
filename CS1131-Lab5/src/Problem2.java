import java.util.Map;
import java.util.Collections;
import java.util.Set;
import java.util.TreeSet;

/**
 *
 * @author Brett Kriz
 */
public class Problem2 {
    /**
* Fixes the given road map by adding symmetric connections
* and ensuring symmetric connections have the same distances.
*
* @param roadmap the road map to fix
*/
    public static void symmetrize(Map <String , Map <String , Double >> roadmap){
        if (roadmap.isEmpty()){
            return;
        }
        // Get a key list an iterate
        Set<String> ks = roadmap.keySet();
        for(int x = 0; x < ks.size(); x++){
            
        }
        
    }
}
